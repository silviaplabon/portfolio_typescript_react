export type Blog = {
  name: string;
  link: string;
  image: string;
  id: number;
  published_date: string;
  subtitle: string;
};
export type Contact = {
  name: string;
  imageLink: string;
  title: string;
  color: string;
  icon: React.ReactNode;
};
export type SkillArea = {
  name: string;
  image: string;
  subtitle: string;
};
